# Fork assessment — kumar-aamit/DemoBot, September 2026

**Date:** 2026-09-09 · **Fork:** [`kumar-aamit/DemoBot`](https://github.com/kumar-aamit/DemoBot) `main` @ `5918842`
(7 commits over merge-base `c8310b7`, PR #65, v4.8.1) · **Target:** [`mayeack/DemoBot`](https://github.com/mayeack/DemoBot)
`main` @ `9bc7aa6` (PR #68, v4.9.0) · **Fork author:** Amit Kumar (`amkum2@cisco.com`) ·
**GitHub compare:** diverged, ahead 7 / behind 5, no PR in either direction ·
**Outcome:** 3 commits kept, 1 kept in part, 2 reworked, 1 rejected; lands as three PRs and ships as 4.10.0.

## Summary

- **What the fork set out to do:** run the app on an on-prem OpenShift cluster behind a Cisco AI Defense
  gateway, drive it from a remote Nemotron served through Ray Serve, keep long chat turns alive through
  proxies, and rebrand the product from DemoBot to **PseudoCo Assistant** for that customer.
- **What is taken as-is:** the SSE heartbeat (`5918842`) and the OpenShift routing decision — a remote NIM is
  reached through `provider=openai` with `OPENAI_REASONING=False`, because `provider=nvidia` is loopback-only
  by design (`backend/nvidia_nim.py`). The `openai_reasoning` flag from `30e8336` is kept and completed
  (Settings field, `.env.example`, tests, and a cache-key fix).
- **What is reworked:** the OpenShift manifests (build from the upstream repo with the shared `Containerfile`,
  no duplicate `Dockerfile`, the 3,119-line CA snapshot replaced by the 2-cert ingress chain plus a
  `ca-bundle` init container) and the rename, which the fork did as a blanket search-and-replace across
  148 files. That sed renamed ~40 load-bearing identifiers *and* corrupted addresses of things outside the
  repo (`REPO_URL="https://github.com/mayeack/PseudoCo Assistant.git"` in the EC2 bootstrap, the
  `/Applications/DemoBot` and `~/DemoBot` checkout paths, `~/.ssh/demobot_ec2`). The rename is redone as a
  scripted, re-runnable pass with an explicit exclusion list — the user chose the full identifier rename, so
  every external object it forces to be re-pointed is listed in §7.
- **What is rejected:** de-registering the NVIDIA AI Virtual Assistant blueprint (7 failures in
  `tests/test_nvidia_blueprint.py`; the parity matrix silently dropped from two blueprints to one),
  hard-coding `hidden` on two Demo Controls cards and disabling NemoClaw polling (replaced by a proper
  per-card visibility panel on the Settings page), deleting `openclaw/Containerfile` (an accident of the
  endpoint agent on the maintainer's Mac), and renaming the blueprint keys as governance identity.
- **Why not `git merge`:** a dry-run merge conflicts in 21 files, all rebrand-versus-PR #66/#68, and the
  three dangerous hunks (blueprint de-registration, module rename, Containerfile deletion) would have
  auto-merged silently.

## 1. Fork commits and verdicts

| Commit | Date | Subject | What it does | Verdict |
|---|---|---|---|---|
| `aed2029` | 2026-09-07 | Local OpenShift deployment config and fixes | `Dockerfile` (arbitrary-UID variant of `Containerfile`), `.dockerignore` (byte copy of `.containerignore`), `deploy/openshift/{configmap,deployment,secret.example.env}`, three CA files at the repo root (`aid-ca.pem` = `aid-chain.pem`, 2 certs; `combined-ca.pem`, 127 certs / 3,119 lines), `.gitignore` line added twice | **Kept, reworked** (PR-A): UID changes folded into `Containerfile`; chain kept as `deploy/openshift/certs/aid-ingress-chain.pem`; snapshot bundle and duplicates dropped |
| `30e8336` | 2026-09-08 | saif updates | (a) comments out the `nvidia_virtual_assistant` blueprint registration; (b) `Settings.openai_reasoning` + `chat_template_kwargs.enable_thinking` for `provider=openai` on non-`api.openai.com` endpoints + cache key; (c) `class="hidden"` on the NeMo Guardrails and NemoClaw cards and on the Blueprint label PR #66 had already removed; (d) `startNemoClawPolling()` commented out; (e) deletes `openclaw/Containerfile` | **(b) kept** as its own commit, author preserved, then completed. **(a), (c), (d), (e) rejected** |
| `87e8ae6` | 2026-09-08 | rebrand: DemoBot -> PseudoCo Assistant | Case-preserving sed over 148 files plus 10 file renames; changes `OTEL_SERVICE_NAME`, the `demobot.*` span attributes and tracer, governance `service_name`/`deployment_id`, AI Defense `src_app`, Agent Control agent/step names, the NemoClaw/OpenClaw plugin id and `DEMOBOT_*` env, `DemoBotState`, `app_name`, launchd/EC2 plumbing, skill files, the detector script | **Reworked** (PR-B): regenerated by script on current `main` with exclusions; the fork's hunks are not cherry-picked |
| `1800d12` | 2026-09-08 | rebrand: rename blueprint keys | `demobot_multi_agent` → `pseudoco_multi_agent` (module, `KEY`, `workflow_name`, config defaults, `.env.example`, tests); NVIDIA `workflow_name` → `pseudoco_nvidia_virtual_assistant` | **Rejected as a cherry-pick**; the same rename is produced by the PR-B script, with the consequences in §7 |
| `4ad5161` | 2026-09-08 | openshift: make deploy/openshift self-sufficient | `buildconfig.yaml` (ImageStream + BuildConfig, git URI pointing at the fork, `dockerfilePath: Dockerfile`), ConfigMap switched from a local NIM to `AI_PROVIDER=openai` against Ray Serve with `OPENAI_REASONING=False`, Deployment mounts an `aid-ca` ConfigMap with `SSL_CERT_FILE`/`REQUESTS_CA_BUNDLE` | **Kept, reworked** (PR-A): URI → `mayeack/DemoBot`, `dockerfilePath: Containerfile`, ImageStream-local image reference, init-container CA bundle, README |
| `aafe15e` | 2026-09-08 | openshift: drop ConfigChange trigger from buildconfig | `triggers: []` so `oc start-build --follow` is the only build | **Kept** (in the manifests commit) |
| `5918842` | 2026-09-08 | sse fix | `backend/routers/chat.py`: thread → `asyncio.Queue` pump and a `: ping` SSE comment after 10 s of silence, so proxies do not drop a long LLM turn (which made the UI replay the turn against `/api/chat/message`, doubling inference and governance events) | **Kept verbatim** (`git cherry-pick -x`) |

## 2. Test evidence

Run with the project venv (`PY=./venv/bin/python`) on both trees, 2026-09-09:

| Suite | Fork `5918842` | Upstream `9bc7aa6` |
|---|---|---|
| `tests/test_nvidia_blueprint.py` | **7 failed** (agents ran primary → triage → responder; agent_trace roles; attribution to the NVIDIA workflow; token sums; HandleOtherTalk; Multi-Agent Mode; degraded reply) | ok |
| `tests/test_blueprint_parity.py` | ok, but the matrix ran for **one** blueprint (`pseudoco_multi_agent`) | ok, both blueprints (80 scenarios each) |
| `tests/test_api.py` | ok | ok |
| `tests/test_synthetic_content.py` | ok | ok |

The parity suite passing on the fork is the trap: it iterates the *registered* blueprints, so
de-registering one halves the coverage without a failure. CLAUDE.md "Blueprint feature parity" requires
both cores registered.

## 3. Merge-conflict inventory (direct merge, rejected)

`git merge-tree` of the fork into `main` conflicts in 21 files: `.claude/skills/{provision-tokens,spin-up-ec2,wire-tunnel-logs-to-o11y}/SKILL.md`,
`.claude/skills/fan-tunnel-to-galileo/{SKILL.md,reference/otel-galileo-pipeline.yaml}` (deleted by PR #68, modified by the sed),
`.env.example`, `CLAUDE.md`, `backend/config.py`, `deploy/ec2/README.{md,html}`, `deploy/ec2/ec2-bootstrap.sh`,
`deploy/endpoints/EC2-*Runbook.{md,html}` (renamed by the fork, edited upstream), `deploy/endpoints/README.{md,html}`,
`deploy/launchd/com.yeack.medadvice-app.plist`, `docs/nvidia-integration.{md,html}`, `docs/observability-fleet-distribution.{md,html}`,
`frontend/index.html`. Every conflict is the rebrand colliding with PR #66 (Blueprint picker removed) or PR #68
(splunk-ao SDK, `GALILEO_*` → `SPLUNK_AO_*`, skill renamed). Everything else auto-merges — including the three
hunks that must not land.

## 4. Identifier impact of the full rename

The user chose the fork's scope (identifiers included). These are the identifiers that change and what keys on them;
§7 is the resulting operator checklist.

| Identifier (old → new) | Where it lives | What keys on it |
|---|---|---|
| APM service `demobot-v3` → `pseudoco-assistant`; tracer `demobot.agents`; span attributes `demobot.theme/.blueprint/.request_id/.trace_id/.agent_surface/.tool.*/.guardrail.rule_names/.ai_defense.event_id` → `pseudoco-assistant.*` | `backend/config.py`, `backend/telemetry/otel.py`, `.env.example` | Splunk Observability Cloud APM service, the two detectors from `scripts/observability/create_apm_detectors.py`, dashboards filtering on `demobot.*` |
| Governance identity `service_name` `demobot-v3`, `deployment_id` `demobot-v3-prod`, `workflow_name`/`blueprint` `demobot_multi_agent`, `demobot_nvidia_virtual_assistant`, `demobot-<theme>` spray identities | `backend/logging/log_schemas.py`, `backend/models/db_models.py`, `backend/agents/blueprints/*`, `backend/spray_campaign.py` | Splunk searches and dashboards on the `gen_ai:json` sourcetype; old events keep old values |
| Agent Control agent `demobot-agent`, step `demobot-llm`, console controls `DemoBot-block-*` | `backend/config.py`, `scripts/demo/register_agent_control.py`, `scripts/demo/controls/*.json` | The agent registered in the Agent Control console and its attached controls |
| Agent Observability project / agent stream default `DemoBot` | `backend/agent_observability.py`, `run-collector.sh` | A box without `SPLUNK_AO_PROJECT` in `.env` starts a new project on first ingest (the Mac pins `DemoBot`) |
| AI Defense `src_app` `demobot-v3` / `demobot-openclaw` | `backend/services/ai_defense.py`, `backend/routers/toolguard.py` | Event attribution in Security Cloud Control |
| Splunk sourcetype `demobot:otel` | `otel-collector-logs.yaml` | Index props and searches (overlay retired 2026-07-30; historical only) |
| OpenClaw/NemoClaw plugin `demobot-toolguard`, `/opt/demobot-plugins`, `DEMOBOT_GUARD_URL/ACCESS_KEY/FAIL_OPEN`, seat `demobot-guard`, images `demobot-openclaw` / `demobot-nemoclaw` | `openclaw/plugins/`, `nemoclaw/`, `guardrails/nemoclaw/policy.yaml`, `run-openclaw.sh`, `run-nemoclaw.sh` | Built images and sandbox configs; rebuilt from HEAD |
| EC2 units `demobot-{app,collector,tunnel,nim,nemoclaw}`, `/etc/demobot-*.env` | `deploy/ec2/ec2-bootstrap.sh` | Existing boxes keep the old units until re-bootstrapped; the bootstrap removes legacy units first |
| `class DemoBotState`, `app_name "DemoBot v4"`, launchd `__DEMOBOT_DIR__` | code, plists | Code identity; `/health` and `/docs` titles |

**Never renamed (exclusions):** the repo URL `github.com/mayeack/DemoBot`, the checkout paths `/Applications/DemoBot` and `~/DemoBot`,
`~/.demobot-openclaw`, `~/.demobot-nemoclaw`, `~/DemoBotDecoy`, `~/.ssh/demobot_ec2`, `~/demobot-payload`, the cloudflared tunnels
`demobot-<n>`, the EC2 tags `demobot-fleet` / `Name=demobot-N` / `Project=DemoBot`, IAM `DemoBotSchedulerRole` / `DemoBotFleetPower`,
the `splunk_ao` package and `SPLUNK_AO_*` / `GALILEO_AGENT_CONTROL_*` env contract, the `medadvice*` names (database, launchd labels,
localStorage keys, tunnel hostnames), theme names. Each is an address of state that is not rebuilt from the repo.

## 5. Strategy

**Chosen:** fresh branches off `main`; `git cherry-pick -x` for the verbatim commit (SSE heartbeat, author Amit Kumar), a partial
pick for the reasoning flag (author preserved), fresh commits with `Co-authored-by` trailers for the reworked OpenShift material,
and a scripted rename generated on the merged tree. History carries only the accepted content; the fork commits are referenced
from each commit body.

**Rejected:** merging the fork branch and resolving the 21 conflicts by hand — it would carry the blanket sed and the four
regressions into `main` and still need a second rename pass for text PR #66/#68 added after the fork.

## 6. PR breakdown, version, tag

| PR | Branch | Content | Order |
|---|---|---|---|
| A | `feat/fork-kumar-aamit` | SSE heartbeat; `OPENAI_REASONING` (flag, Settings field, `.env.example`, host-capability text, tests, cache-key fix); `Containerfile` arbitrary-UID; `deploy/openshift/` manifests, chain cert, README; doc pointers; this assessment | first |
| C | `feat/demo-controls-visibility` | Settings-page "Demo Controls" panel: server-side registry `settings_store.DEMO_CONTROLS`, `GET/PUT /api/settings/demo-controls`, `data-control` hooks on the 14 drawer cards, chat page hides cards and empty groups, hidden per-request cards send no override, CLAUDE.md sync rule, `tests/test_demo_controls.py` | second |
| B | `feat/rename-pseudoco-assistant` | Scripted rename (rules + exclusions in the commit body), EC2 legacy-unit migration in the bootstrap, CLAUDE.md "Product naming" rewrite, `docs/RELEASING.md` mirror, version 4.10.0 | last |

After PR-B merges: annotated tag `v4.10.0` from `main`, `gh release create` (`docs/RELEASING.md`).

## 7. Operator checklist after PR-B (nothing here is automated)

1. **Per box, decide the telemetry identity.** The Mac's `.env` pins `OTEL_SERVICE_NAME=demobot-v3`,
   `deployment.environment=demobot-local`, `SPLUNK_AO_PROJECT=DemoBot`, `SPLUNK_AO_AGENT_STREAM=DemoBot`; those keys win over
   the new code defaults. Remove or change them per box to move that box to the new names.
2. **Splunk Observability Cloud:** re-create the two APM detectors on the new service with
   `scripts/observability/create_apm_detectors.py` (the old ones stay on `demobot-v3` until deleted); update dashboards that
   filter on `demobot.*` span attributes or the old environment names.
3. **Splunk Enterprise (`gen_ai:json`):** searches and dashboards keyed on `service_name`, `deployment_id`, `workflow_name`,
   `blueprint` need the new values from the cut-over time; historical events keep the old ones.
4. **Agent Control:** ~~re-register the agent~~ **Done 2026-09-09.** `scripts/demo/register_agent_control.py` created
   agent `pseudoco-assistant-agent` (step `pseudoco-assistant-llm`) and attached controls 259/263/576; `demobot-agent` is
   left in place. Until this ran, `GET /api/v1/agents/pseudoco-assistant-agent/controls` returned **404** and the guardrail
   loaded no definitions at all, so it failed open on every turn. The three controls still carry their original
   `DemoBot-block-*` names — renaming them to `PseudoCoAssistant-block-*` is outstanding and needs a console-side
   `PATCH /api/v1/controls/{id}`. Enforcement is partial: 263 (output PII) blocks in-process, while 576 and 259 still fail
   their client-side Luna scorers (see the project notes on the org's missing runtime-token grant).
5. **Agent Observability:** keep `SPLUNK_AO_PROJECT`/`SPLUNK_AO_AGENT_STREAM` pinned where the existing `DemoBot` project
   should keep receiving traces; unset them where a new project is wanted.
6. **AI Defense:** `src_app` changes; nothing to configure, but filters on the old value stop matching.
7. **OpenClaw / NemoClaw:** rebuild the images (`run-openclaw.sh`, `run-nemoclaw.sh`); the plugin id, install path,
   build args and the network-policy preset all carry the new names. The decoy workspace and gateway state dirs are excluded
   from the rename and stay where they are.
8. **EC2:** fresh bootstraps install `pseudoco-assistant-*` units; re-running the bootstrap on an existing box stops, disables
   and removes the legacy `demobot-*` units first. Replica scripts resolve whichever app unit is installed.
9. **OpenShift:** the manifests in PR-A already use the new names; nothing changes at PR-B.

## 8. Verification

```bash
cd /Applications/DemoBot
PY=./venv/bin/python tests/run_all.sh                 # every PR; all suites RESULT: ok
./venv/bin/python -c "from backend.agents.blueprints import BLUEPRINTS; print(sorted(BLUEPRINTS))"   # both blueprints
git ls-tree HEAD openclaw/Containerfile               # present (never `test -f` on the Mac: the dir is sparse-excluded)
./venv/bin/python -c 'import yaml,glob; [list(yaml.safe_load_all(open(f))) for f in glob.glob("deploy/openshift/*.yaml")]'
podman build -f Containerfile -t demobot-test . && podman run --rm --user 1000123456:0 demobot-test sh -c 'umask 0002 && touch /app/data/z && echo ARBITRARY_UID_OK'
# after PR-B: only the exclusions may still say demobot (the list is in the PR-B commit body)
git grep -n -i 'demobot' -- . ':!docs/fork-assessment-*' | grep -v -E 'github.com/(mayeack|kumar-aamit)/DemoBot|DemoBot\.git|/Applications/DemoBot|\$HOME/DemoBot|~/DemoBot|-Applications-DemoBot|\.demobot-(openclaw|nemoclaw)|DemoBotDecoy|demobot_ec2|demobot-payload|demobot-\$|demobot-[0-9]|demobot-fleet|Project=DemoBot|DemoBotSchedulerRole|DemoBotFleetPower'
```

## 9. Follow-ups (out of scope)

The operator checklist itself; an optional GitHub repository rename (old URLs redirect); a Settings-page card for
`openai_reasoning` beyond the provider field; PR #67 (control 576) has no overlap with any of the three PRs.
