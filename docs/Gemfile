# Optional — for local preview only. GitHub Pages builds this site server-side
# from the `remote_theme` in _config.yml and does NOT use this Gemfile.
#
#   cd docs && bundle install && bundle exec jekyll serve
#   open http://127.0.0.1:4000/DemoBot/
#
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache", group: :jekyll_plugins

# Windows / JRuby file-watching helpers (harmless elsewhere).
gem "wdm", "~> 0.1", platforms: [:mingw, :x64_mingw, :mswin]
gem "webrick", "~> 1.8"
